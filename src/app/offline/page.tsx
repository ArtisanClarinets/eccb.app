import { OfflineContent } from '@/components/offline-content';

export const metadata = {
  title: 'Offline | Emerald Coast Community Band',
};

export default function OfflinePage() {
  return <OfflineContent />;
}
