Code style: TypeScript strict mode, explicit return types for exported functions, prefer interfaces, avoid any. Imports grouped React/external/internal and sorted alphabetically. Formatting: 2 spaces, single quotes, trailing commas, print width 100. Naming: PascalCase components, use* hooks, camelCase utils, SCREAMING_SNAKE_CASE constants. Use absolute imports from src/. Tailwind with cn() utility and project design tokens.